# Iris Data Analysis

Steps to run:
1. Create and activate the virtualenv.
2. Install dependencies: `pip install -r requirements.txt`
3. Run analysis: `python analysis.py`
Outputs saved to `figures/` and `summaries/`.
